import React from 'react'
import { View, Text } from 'react-native'

const Title = ({title}) => {
    return (
        <View style={{}}>
            <Text style={{color:"#1565C0"}}>{title}</Text>
        </View>
    )
}

export default Title
